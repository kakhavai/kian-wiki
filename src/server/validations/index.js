import { projectValidation } from './project.validation';
import { userValidation } from './user.validation';

const options = { keyByField: true };

export { projectValidation, userValidation, options };
